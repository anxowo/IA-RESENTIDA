services:
  opencloud:
    image: owncloud/ocis:latest
    container_name: opencloud

    user: "1000:1000"

    ports:
      - "9200:9200"

    environment:
      OCIS_INSECURE: "true"
      OCIS_URL: "http://26.195.60.25:9200"
      OCIS_CONFIG_FILE: "/etc/ocis/ocis.yaml"
      HOME: "/var/lib/ocis"

    volumes:
      - ./data:/var/lib/ocis
      - ./data/config:/etc/ocis

    command: server

    restart: unless-stopped

